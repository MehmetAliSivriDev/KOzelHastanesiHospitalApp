import 'package:flutter/material.dart';

class ProductBorderRadius extends BorderRadius {
  const ProductBorderRadius.txtFormFieldBRadius()
      : super.all(const Radius.circular(30));
  const ProductBorderRadius.lTileContainerBRadius()
      : super.all(const Radius.circular(20));
  const ProductBorderRadius.dropdownBRadius()
      : super.all(const Radius.circular(15));
}
